# ----------------------------------------
# ENV FILE USAGE
# ----------------------------------------
# - Copy this file to `.env` for shared defaults.
# - Create `.env.development`, `.env.staging`, or `.env.production` to override per environment.
# - `NODE_ENV` selects the file (defaults to `development`); `.env.<NODE_ENV>` values override `.env`.
# - If no env-specific file exists, `.env` is used by itself.
# ----------------------------------------
# ----------------REQUIRED----------------
# ----------------------------------------

NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://localhost:27017/test


# Network / CORS
CORS_ALLOWED_ORIGINS=
CORS_ALLOWED_REGEX=.*
# -----Set true for postman and false for production
CORS_ALLOW_NO_ORIGIN=true

# Core Server
BACKEND_URL=http://localhost:5000
TRUST_PROXY=1

# ----------------------------------------
# ----------------OPTIONAL----------------
# ----------------------------------------

# Feature Flags
ENABLE_RATE_LIMIT=
ENABLE_MORGAN_LOGS=

# Security & Auth
# Keycloak
KEYCLOAK_JWKS_URI=https://keycloak.example.com/realms/myrealm/protocol/openid-connect/certs
KEYCLOAK_ISSUER=https://keycloak.example.com/realms/myrealm
KEYCLOAK_AUDIENCE=account

# Database (Mongo Tuning)
MONGODB_MAX_POOL_SIZE=
MONGODB_MIN_POOL_SIZE=
MONGODB_SERVER_SELECTION_TIMEOUT_MS=
MONGODB_WAIT_QUEUE_TIMEOUT_MS=
MONGODB_SOCKET_TIMEOUT_MS=
MONGODB_CONNECT_TIMEOUT_MS=
MONGODB_MAX_IDLE_TIME_MS=
MONGODB_AUTO_INDEX=

# Rate Limiting
RATE_LIMIT_WINDOW_MS=
RATE_LIMIT_MAX_REQUESTS=
