import type { Connection } from "mongoose";

export const seedIndexes = async (_connection: Connection) => {
  // Add collection-specific index creation here as modules are added.
  void _connection;
};
